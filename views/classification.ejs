<%- include('./partials/head') %>
<%- include('./partials/header', { classifications, isAuth }) %>

<%
  const priceFormatter = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" })
%>

<main>
  <h1><%= title %></h1>
  
  <div class="vehicles">
    <% vehicles.forEach(vehicle => { %>
      <div class="vehicle-item">
        <img src="<%= vehicle.inv_thumbnail %>" alt="">
        <a href="/inventory-model/<%= vehicle.inv_id %>"><%= vehicle.inv_make %> <%= vehicle.inv_model %> <%= vehicle.inv_year %></a>
        <p><%= priceFormatter.format(vehicle.inv_price) %></p>
      </div>
    <% }) %>
  </div>
</main>

<%- include('./partials/footer') %>
